# PaperVerse


## Installation

```
git clone https://github.com/mingyangli/PaperVerse.git
cd PaperVerse
pip install -r requirements.txt


cd src
uvicorn main:app --reload
```

After activating the server, try to open the index.html in the browser.